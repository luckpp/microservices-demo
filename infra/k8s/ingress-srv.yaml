apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-srv
spec:
  rules:
    - http:
        paths:
          - path: /posts
            backend:
              serviceName: posts-srv
              servicePort: 4000
